import 'package:flutter/material.dart';

class ExperienceCVDesktop extends StatelessWidget {
  const ExperienceCVDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('3D Experience'),
      ),
    );
  }
}
