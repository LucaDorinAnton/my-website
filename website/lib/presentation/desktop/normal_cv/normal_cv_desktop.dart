import 'package:flutter/material.dart';

class NormalCVDesktop extends StatelessWidget {
  const NormalCVDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Normal Experience'),
      ),
    );
  }
}
